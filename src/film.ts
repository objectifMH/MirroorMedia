export class Film {
    // tslint:disable-next-line:max-line-length
    constructor(public id: number, public titre: string, public realisateur: string, public acteurs: string[], public description: string, public cover: string, public date:string) {}
  }
